[project]
name = "rnmanager"
version = "0.1"
description = "Manage random number stream state."
readme = "README.md"
license = {file = "LICENSE"}
authors = [
	{name = "yy-harashima"}
]
dependencies = [
	     "numpy",
]

[tool.setuptools]
package-dir = {"" = "rnmanager"}